# 历史文档归档

这里保存已被当前权威覆盖、但仍需 provenance 的设计、实施计划和 code map。

- `design-history/`：按日期保留的旧设计、修复计划、执行 prompt 与研讨材料。
- `code-maps/`：已被 `Doc/TechnicalDocument/tokenshare_v1_code_map.md` 覆盖的子系统快照。
- `agent-plans/`：已完成或失效的 agent 执行计划。

归档文件中出现“当前”“最新”“唯一权威”只表示该文件产生时的历史状态。Agent 默认不得把这里作为实现依据；只有追查旧 digest、回归语义或 provenance 时才按需读取。

当前事实入口见 `Doc/agent-navigation.md`。
