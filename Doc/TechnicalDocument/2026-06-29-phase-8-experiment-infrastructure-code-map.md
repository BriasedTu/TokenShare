# Phase 8 实验基础设施代码映射

日期：2026-06-29

状态：Phase 8 第一版实验基础设施已实现并完成定向验证。本文映射 `feat-009` 的 source、tests、验证证据和边界说明。2026-07-12 起，本文只作为当前实现事实和新 paper runner 缺口的 code map，不再承担实验设计权威；唯一权威是 `tokenshare_latest_real_plugin_experiment_design.md`。旧默认 Experiment 1-4、scripted AI profile、direct factorization 500 和 Lean AI 50 继续用于 regression/calibration，但缺少三档难度、协议内部 worker scaling、post-AI fault rate、独立 worker death、重复统计和完整 paper report，因此不能直接作为新论文主实验结果。2026-07-14 起，feat-011 Task 1 已补上 paper schema、30+30 catalog、budget plan 和最小 gated CLI；feat-011 Task 2 已补上真实 API paper eligibility 派生 gate，并把 Lean paper catalog 从 metadata-only 升级为本地 Lean checker-backed preflight；feat-011 Task 4 已补上 factorization paper adapter 的 range-child AI executor / parser / verifier / merge 第一版；feat-011 Task 5 已补上 Lean paper adapter 的 split-helper / AI proof candidate / parser / checker / merge-root-recheck 第一版；2026-07-15 feat-011 Task 7 已补上 post-AI fault injection 记录与 mutation artifact 模块；2026-07-15 feat-011 Task 8 已补上独立 worker process death harness、lease expiry 和 replacement attempt 记录；2026-07-15 feat-011 Task 9 已补上 ablation mode/profile、Experiment 4 condition expansion、provider-attempt coverage gate 和 evidence-derived ablation summary；2026-07-15 又补上 `paper_difficulty` schema、Lean v1 simple 映射、Lean v2 lemma-DAG catalog loader、`paper_difficulty_counts` 和 `expected_ai_unit_count` 预算估算；2026-07-15 后续最小题库切片已把 Lean v2 medium fixture 升级为真实 checker-backed oracle package preflight asset，并添加 hard/frontier structured blocked fixture；2026-07-15 schema retrofit 又补上 Lean topic/provenance/proof assembly metadata、`topic_family_counts`、`paper_difficulty_topic_family_counts`、`cases_for(..., topic_family=...)` 和 budget selection 修复，防止 `medium_lemma_dag` 条件混入 shallow v1 legacy medium cases；2026-07-15 本轮最小 checker-backed catalog/preflight fixture 又新增 `function_set` 与 `induction` 两个 `medium_lemma_dag` golden cases；2026-07-16 F 已补上 Lean plugin 层 lemma-DAG proof-file assembly/root recheck，review hardening 后 root proof 不再直接 exact root oracle，而是按 dependency edges 对有入边节点合成推导并再跑 `MERGE_PROOF`；2026-07-16 G 已补上实验层 Lean v2 `medium_lemma_dag` adapter catalog-case execution，按 node 走 `AIAPIExecutor` raw/parsed artifact、`CHILD_PROOF` checker 和 `merge_lean_lemma_graph_proofs()` / `MERGE_PROOF` root recheck；2026-07-16 本轮补上 Lean 3×3 matrix 的 plan-only / blocked-aware gate，CLI 输出 `lean_3x3_matrix.json` 并把 9-cell summary 嵌入 `run_budget.json`，但不启动真实 provider 调用、不把 blocked cell 伪装成可执行；2026-07-16 Task 10 review hardening 后正式 Experiment 5 已改为三模型 fixed-entry model-provider endpoint cohort，新增 frozen cohort 文件 `benchmarks/paper/model_comparison_cohort.v1.json`，preflight 固定校验正式 cohort id、精确三 member 集合、provider/model/reasoning identity、entry-map cohort id、selected entry 和 smoke evidence schema/status/provider/model/reasoning；`expand_plan_conditions()` 只从 planned preflight member plan 展开 18 个单 repeat conditions 并持久化 `model_entry_id`，blocked cohort 展开 0 个 Exp5 condition；正式 `PaperExperimentCondition.model_policy` 只允许 `fixed_entry`，正式 CLI 不再暴露旧 model policy 参数。legacy `strong_only` / `weak_only` / `mixed` resolver 只保留在 `paper_model_policy.py` 内部作为非正式兼容 helper，不导出、不进入 paper schema、正式 CLI、condition、预算或 execution record。OpenAI 官方 `UrlLibOpenAITransport` 已接入 factorization / Lean paper adapters 的 real transport guard；三条 paper adapter request builder 都从 `AIAPIExecutorConfig.provider_family` 写入 `ExecutionRequest.capability_snapshot` 和 `hard_requirements`，且 adapter 会用 `condition.model_entry_id` 绑定唯一 entry。metrics / report、正式 runner/pilot integration、worker scaling condition expansion、formal secret scan wiring、正式故障矩阵 / ablation 矩阵运行、正式 Experiment 1-5 real API runs、per-run `model_execution_records`、3×3 批量题库扩展和 hard/frontier oracle-backed 扩展仍在后续任务中。

## 1. Source Map

| 文件 | 当前依据 | 当前内容 |
|---|---|---|
| `src/tokenshare/experiments/models.py` | 已实现 regression schema | `SimulationProfile`、`ExperimentCase`、`ExperimentRun`、`ExperimentStatus`、adapter preflight / result / runner result 纯模型和稳定 digest helper；新 paper schema 另见权威实验设计。 |
| `src/tokenshare/experiments/paper_models.py` | feat-011 Task 1-2 / paper_difficulty / topic schema / v2 adapter records / Task 10 fixed-entry cohort schema | 新论文实验 condition / suite / experiment / condition / run / task / attempt / budget result schema，包含 stable JSON digest、paper status / task status / attempt status / failure stage / failure kind 枚举；Task 2 新增 `PaperEligibilityReport` 和 `evaluate_paper_eligibility()`，从 `run_evidence` 中的 transport evidence、secret scan report、artifact manifest inventory 和 attempt 级 provider evidence 派生 `paper_eligible`。`PaperAttemptResult` 已补齐 `provider_attempt_index`、`started_at`、`ended_at`、`prompt_tokens`、`completion_tokens`；gate 不接受单独传入的 `real_transport` / `secret_scan_passed` 布尔断言，artifact ref 必须在 manifest inventory 中按 id/hash/type/schema 匹配。2026-07-15 起 `PaperExperimentCondition` 和 `PaperTaskResult` 序列化 `paper_difficulty`，并把它纳入 condition digest；legacy `difficulty=easy|medium|hard` 保留兼容。2026-07-15 schema retrofit 又新增 `topic_family`、`topic_family_version`、`construction_rule_id`、`oracle_package_group`、`proof_assembly_shape`，condition digest 包含这些字段，Lean topic family 只允许 `pure_logic` / `function_set` / `induction`。2026-07-16 G 起 `PaperAttemptResult` 也序列化这些 topic/provenance/proof-assembly 字段，并新增 `lemma_node_id`、`slot_key`、`dependency_path`，让 v2 lemma-DAG node attempts 可回溯到 graph metadata。2026-07-16 Task 10 review hardening 起 `PaperExperimentCondition.model_policy` 在正式 schema 中只接受 `fixed_entry`，并新增 `model_cohort_id`、`cohort_member_id`、`model_entry_id`、`provider_family`、`provider_model_id`、`reasoning_profile_id`、`model_cohort_digest` 字段；旧 strong/weak/mixed 不再能进入 formal condition。`PaperSuiteResult` 保留历史 `model_policy_preflight` 可空字段以读取旧输出，同时携带正式 `model_endpoint_cohort_preflight`。 |
| `src/tokenshare/experiments/paper_model_policy.py` | feat-011 Task 10 fixed-entry cohort / legacy-only helper | 正式 Experiment 5 helper：`load_model_endpoint_cohort()` 读取三模型 fixed-entry cohort，`load_model_entry_map()` 读取 cohort member 到 provider config / entry 的本地映射，`load_provider_config_map()` 加载各 provider family 的 `AIAPIExecutorConfig`，`build_model_endpoint_cohort_preflight()` 固定使用代码内权威三 member identity，不信任 cohort 文件重新定义正确模型；它校验正式 cohort id、member 集合恰好等于 `glm_5_2_siliconflow` / `qwen3_6_27b_siliconflow` / `gpt_5_6_sol_high_openai`、cohort member provider/model/reasoning、entry-map cohort id、provider config family、selected entry model/reasoning、api key env，以及 smoke evidence 的 schema/status/provider/model/reasoning/artifact refs。任一失败输出 `status=blocked`、`blocked_reason=incomplete_model_cohort`、`paper_eligible_possible=false`、`provider_calls_made=0`。旧 `strong_only` / `weak_only` / `mixed` resolver 仍留在本模块作为 legacy-only helper，不导出且不进入正式 paper schema、CLI、budget、condition 或 execution record。 |
| `src/tokenshare/experiments/adapters.py` | 已实现 adapter contract | `PluginExperimentAdapter` 协议和 `AdapterRegistry`，通用 runner 只通过插件 ID / version 查找 adapter。 |
| `src/tokenshare/experiments/factorization_adapter.py` | 旧 default suite | 复用 `run_factorization_fixture_flow()` 生成旧 Experiment 1-4 regression report；不是全 range-child 真实 API paper adapter。 |
| `src/tokenshare/experiments/factorization_paper_adapter.py` | feat-011 Task 4 / Task 10 OpenAI adapter integration | 新论文 factorization adapter 第一版：从 frozen paper catalog case 构造 root subject，调用 factorization 插件 deterministic `candidate_range_partition.v1` 生成 range children，每个 child 通过 `AIAPIExecutor` 生成 raw/parsed/candidate/provenance evidence，经插件 parser / `verify_range_result()` 验证后把 accepted range result 作为 canonical-like slot 输入交给 `merge_required_range_results()`；输出 `FactorizationPaperRunResult`、`PaperTaskResult`、`PaperAttemptResult`、run evidence 和最小 JSONL/manifest。测试 transport 使用 scripted provider 但仍走真实 `AIAPIExecutor` artifact 路径，且被 eligibility gate 标为 paper-ineligible；`real_transport=True` 只接受与 config `provider_family` 匹配的官方 `UrlLibSiliconFlowTransport` 或 `UrlLibOpenAITransport` exact type，不接受任意注入 transport / subclass；OpenAI config 可经 adapter 进入 executor/parser 路径，`ExecutionRequest` artifact 的 `capability_snapshot.provider_family` 和 `hard_requirements.provider_family` 来自当前 config，`condition.model_entry_id` 会在 provider call 前绑定唯一 entry 且显式 mismatch 会失败；root failure stage/kind 从 child attempt evidence 派生，secret scan 在正式 wiring 前记录为 `pending`。 |
| `src/tokenshare/experiments/lean_adapter.py` | 旧 default suite | 运行真实 Lean preflight/checker fixture，保留 blocked 回归；不提供三档难度、真实 API worker scaling、fault rate 或 ablation paper matrix。 |
| `src/tokenshare/experiments/simulation.py` | regression simulation v1 | `SimulationWrapper` 只记录五类故障和消融决策，不执行新设计要求的 post-AI mutation 或 worker process death。 |
| `src/tokenshare/experiments/paper_faults.py` | feat-011 Task 7 | 新论文 post-AI fault injection 模块：`select_fault_targets()` 保留 deterministic seed + AI unit id 兼容入口；`select_fault_target_descriptors()` 支持 generic `FaultTargetDescriptor`，可记录 `slot_key`、`child_logical_key`、`dependency_path`、`lemma_node_id`、`parent_node_id`、`merge_slot`、`artifact_ref` 和 domain-specific target metadata，后续 Lean lemma-DAG / factorization range fault 可复用同一 target schema。`inject_post_ai_fault()` 要求 raw output artifact 已持久化后才写 mutation artifact 和 `FaultInjectionRecord`，record 与 mutation artifact 同时写入 `target_context` / `target_selection_digest`，覆盖 `false_positive`、`false_negative`、`no_return`、`late_submission`、`executor_error` 五类非死亡故障，并返回带 `fault_injection_ref` 的 mutated `PaperAttemptResult`。`false_negative` 只允许作用于确实存在 found factor 或非空 Lean proof 的 parsed payload。synthetic mutation 的 `provider_tokens_attributed=0`，恢复类 fault 只记录 `retry_required`，后续恢复 attempt 必须由 runner 再次真实调用 API。 |
| `src/tokenshare/experiments/paper_workers.py` | feat-011 Task 8 | 新论文 worker death harness 模块：`PaperAIUnit` / dependency graph snapshot 按 generic AI unit 和 dependency edges 建模；`validate_ai_unit_dependency_graph()` 校验 missing dependency、cycle、唯一 unit id、同一 task id、实际 DAG depth 与 unit 声明 depth 一致，并生成可复算 `graph_digest`；`run_worker_death_harness()` 启动独立 subprocess worker，在 `progress_25` / `progress_50` / `progress_75` kill point 终止 worker，coordinator 通过现有 `LeaseManager.expire()` 等待 lease expiry，再 claim replacement worker attempt，并把 worker pid、kill point、expired lease、recovery action、reassignment、replacement attempt 和 dependency graph digest 写入 `WorkerDeathRecord` artifact。模块不调用真实 provider、不复用故障输出、不修改协议 core，也不假设 Lean 两 child / `P ∧ Q` / `P ↔ Q` / 单层 child proof。 |
| `src/tokenshare/experiments/paper_ablation.py` | feat-011 Task 9 | 新论文 ablation support 模块：定义 `PaperAblationMode`（FULL、NO_PARSER_POLICY、NO_VERIFICATION、NO_REQUEUE、NO_MERGE_GATE、NO_SLOT_INTEGRITY）、`PaperAblationProfile`、`AblationAttemptCoverage`、`PaperAblationEvidenceRecord` 和 `PaperAblationSummary`。profile 固定并强制 `scope=experiment_boundary`、`requires_provider_attempt_before_ablation=true`、`system_default_behavior_changed=false`，确保关闭机制只发生在实验边界；attempt coverage 按 generic dependency graph 的 AI unit IDs 检查每个 unit 已有 provider attempt，重算并校验 dependency graph digest，并用 artifact inventory 校验 request / raw / provenance / usage / parsed-or-parse-failure refs 的 id/hash/type/schema/source，拒绝 artifact-id-only fabricated refs；summary 只从带 event refs 和完整 artifact refs 的 evidence records 复算 canonical pollution、premature merge、blocked root、settlement blocked 和 slot integrity violation，且 metric flags 必须是 bool，不采信硬写 0/1 指标或字符串布尔值。 |
| `src/tokenshare/experiments/metrics.py` | regression metrics v1 | 可从 copied JSONL/artifacts 复算部分指标，但仍有 hard-coded 0/1 字段；新 paper metrics 不得直接采信这些固定值。 |
| `src/tokenshare/experiments/report.py` | regression report v1 | 写单 run JSON/CSV；缺少 condition/repeat aggregation、paper eligibility、budget、图表 CSV、secret scan report。 |
| `src/tokenshare/experiments/paper_catalog.py` | feat-011 Task 1-2 / Task 5 hardening / paper_difficulty / topic family / Lean lemma-DAG preflight slice | 读取并校验 frozen paper catalog：factorization / Lean 各 30 个 case、easy/medium/hard 各 10 个、case ID 去重、factorization oracle/range、catalog manifest digest；Task 2 新增本地 Lean checker-backed catalog preflight，使用 embedded `oracle_proof_ref.proof_source` 构造 proof candidate，经固定本地 Lean/lake checker 验证 30 个 Lean theorem payload，并在 manifest 写入 `lean_preflight_status=passed` 与 `lean_preflight_summary`。Task 5 暴露并修复 Lean case 行级 `environment_digest` 与 checker-backed preflight manifest 漂移：loader 现在要求每个 Lean case 的 `environment_digest` 匹配当前 checker environment，并公开 `lean_theorem_payload_from_case()` 与 `default_lean_paper_environment_manifest()` 给 paper adapter 复用。2026-07-15 起 loader 会在内存中把 factorization `paper_difficulty` 映射为 easy/medium/hard、把当前 Lean v1 全部映射为 simple/pure_logic，并可通过 `lean_lemma_graph_path` 加载 Lean v2 lemma graph catalog；manifest 新增 `paper_difficulty_counts`、`topic_family_counts`、`paper_difficulty_topic_family_counts`、`lean_lemma_graph_cases` 和 `lean_lemma_graph_preflight_summary`。v2 validator 校验 required fields、topic family / version、construction rule id、oracle package group、supported proof assembly shape、node theorem payload / statement / node kind / declared depth、dependency edge 引用、DAG 无环、expected depth/leaf/AI-unit count、medium oracle/environment/preflight 和 hard-frontier no-oracle blocked/stress 约束。`cases_for()` 支持按 domain、可选 legacy difficulty、paper_difficulty、topic_family 组合过滤。medium oracle package preflight 会校验 fixed package source hash、构建 `TokenShare.LemmaGraphOracle`，并对每个 graph node 构造 `LeanTheoremPayload` + oracle proof candidate 后调用现有 `check_lean_proof()`；hard/frontier 无 oracle 只计 structured blocked，不调用 checker 伪造 success。2026-07-15 本轮仅为 induction oracle-preflight 增加 `recursive_induction_lemma_dag_required_slots.v1` 的 catalog-loader 允许项，不实现 adapter execution。该 loader 不调用 Lean split plugin，也不执行 adapter DAG。 |
| `src/tokenshare/experiments/paper_budget.py` | feat-011 Task 1 / Task 9 / paper_difficulty / topic selection slice / 3×3 plan gate / Task 10 endpoint cohort evidence | 生成 plan-only budget，估算 root runs / AI units / provider attempts / token / cost / wall-clock，并强制非 plan-only run 必须携带匹配的 `approve_budget_digest`。Task 9 起对 Experiment 2 / 4 / 5 使用每个 domain / paper difficulty 固定 5-task batch 估算，Experiment 4 单 repeat 为 180 root-runs，3 repeats 对齐权威设计的 540 root-runs。2026-07-15 起显式 `paper_difficulty` condition 会过滤对应 catalog tier，且 case 含 `expected_ai_unit_count` 时预算优先使用该字段。2026-07-15 schema retrofit 修复 budget selection：把 `condition.paper_difficulty` 和 `condition.topic_family` 传给 catalog selection；若指定 topic cell 缺 case，抛出 `insufficient catalog`，不再 fallback 到 legacy shallow v1 difficulty。2026-07-16 起可接收 Lean 3×3 matrix plan，将其纳入 budget digest并写入 `quota_preflight.lean_3x3_matrix`；该 matrix 仅是 plan-only 证据，不把 blocked cell 计为正式 provider attempts。Task 10 review fix 起可把正式 `model_endpoint_cohort_preflight` 纳入 budget digest 和 `quota_preflight`，保持 `provider_calls_made=0`；旧 `model_policy_preflight` 只保留兼容字段。 |
| `src/tokenshare/experiments/paper_runner.py` | feat-011 Task 1 / Task 9 / paper_difficulty / topic metadata slice / 3×3 plan gate / Task 10 fixed-entry cohort expansion | 提供 paper experiment id normalization 和 plan condition expansion；已覆盖 `exp1_real_ai_feasibility` 的 domain × difficulty × repeat planning，以及 `exp4_real_ai_protocol_ablation` 的 domain × difficulty × 6 ablation modes × repeat planning。当前 shallow Lean v1 的 `lean_proof` medium / hard exp4 条件会标记 `paper_eligible_required=false`，直到补齐 medium recursive lemma-DAG catalog 和对应 split/merge/adapter 能力；当前仍未串起正式真实 API ablation run 执行、paper report 或 CSV 输出。2026-07-15 起 plan conditions 会显式携带 `paper_difficulty`：factorization 为 easy/medium/hard，当前 Lean v1 为 simple；schema retrofit 后当前 Lean v1 plan conditions 还显式带 `topic_family=pure_logic` / `topic_family_version=shallow_v1`。2026-07-16 起新增 `build_lean_3x3_matrix_plan()`，固定输出 `simple/medium_lemma_dag/hard_frontier` × `pure_logic/function_set/induction` 9 个 cell，记录 checker-backed available count、target count、expected AI units、preflight status summary、`paper_eligible_possible` 和 `blocked_reason`；当前只有 `simple/pure_logic` 达到 target，medium 三个 cell 均 insufficient，hard/frontier no-oracle 只 structured_blocked。Task 10 review hardening 起 `exp5` alias 映射到 `exp5_real_ai_model_endpoint_comparison`，并且只从 `model_endpoint_cohort_preflight.status=planned` 的三条固定 member plan 展开 conditions；每个 condition 持久化 selected `model_entry_id`、cohort/member/provider/model/reasoning metadata 和 cohort digest。blocked / extra-member / missing-member cohort 展开 0 个 Exp5 condition；`exp1` / `exp4` 使用 `fixed_entry` default 但不依赖 Exp5 cohort preflight。 |
| `src/tokenshare/experiments/runner.py` | 旧 default suite | `run_phase8_default_suite()` 固定 deterministic local Experiment 1-4，只用于 regression。 |
| `src/tokenshare/experiments/ai_profile.py` | 2026-06-30 calibration | deterministic/scripted/optional-real profiles，用于 executor/parser/cost 校准；不满足新 paper suite 全 real hard gate。 |
| `src/tokenshare/experiments/run_ai_profile.py` | calibration CLI | AI profile 命令入口；不是新论文入口。 |
| `src/tokenshare/experiments/factorization_500_ai.py` | 2026-07-02 direct model benchmark | 500 个 semiprime direct answer，真实 API 可用，但并发独立整数且绕过协议 range lifecycle；仅作输入/oracle/成本校准。 |
| `src/tokenshare/experiments/run_factorization_500_ai.py` | direct benchmark CLI | 支持真实模型和并发；不是协议 worker scaling CLI。 |
| `src/tokenshare/experiments/lean_ai_benchmark.py` | 2026-07-02 shallow Lean benchmark | 25 conjunction + 25 iff，支持真实 API/checker/merge，但没有三档难度、condition/repeat/fault/ablation paper matrix。 |
| `src/tokenshare/experiments/lean_paper_adapter.py` | feat-011 Task 5 / 2026-07-16 G / Task 10 OpenAI adapter integration | 新论文 Lean adapter 第一版保留 v1 shallow 路径：从 frozen Lean paper catalog case 构造 theorem payload，复用 Lean 插件 split helper 生成 split certificate / structured unsupported，按 child payload 通过 `AIAPIExecutor` 生成 proof candidate raw/parsed/provenance/usage evidence，经 Lean plugin parser 和本地 checker生成 checker report / proof artifact / environment digest，再用 `merge_lean_child_proofs()` 做 child proof merge 和 root recheck。2026-07-16 G 新增 v2 `tokenshare.paper_lean_lemma_graph_case.v1` dispatch：`medium_lemma_dag` case 不调用 v1 `run_lean_split_helper()` 解析 theorem shape，而是从 catalog deterministic graph 构造并保存 `LeanLemmaGraphCertificate`，调用 `build_lean_split_plan()`，对每个 lemma node 以 `lean_proof_lemma_node` unit 走 `AIAPIExecutor` raw output artifact、parsed proof candidate artifact、usage/provenance evidence 和 `LeanCheckerMode.CHILD_PROOF` checker；accepted nodes 再调用 `merge_lean_lemma_graph_proofs()` 做 dependency-aware assembled proof + `MERGE_PROOF` root recheck。child checker rejected、parse failure、provider failure 均阻止 fake merge success；hard/frontier no-oracle case 返回 structured blocked evidence、0 provider calls 和 `accepted_validity=false`。输出 `LeanPaperRunResult`、`PaperTaskResult`、`PaperAttemptResult`、node checker evidence、merge summary、run evidence 和最小 JSONL/manifest；records 带 `lemma_node_id`、`slot_key`、`dependency_path`、`paper_difficulty`、`topic_family`、`construction_rule_id`、`oracle_package_group`、`proof_assembly_shape`。测试 transport 使用 scripted provider 但仍走真实 `AIAPIExecutor` artifact 路径，且被 eligibility gate 标为 paper-ineligible；`real_transport=True` 只接受与 config `provider_family` 匹配的官方 `UrlLibSiliconFlowTransport` 或 `UrlLibOpenAITransport` exact type，不接受任意注入 transport / subclass。v1 child 和 v2 lemma node 的 `ExecutionRequest` artifact 都从当前 config 写真实 provider family，`condition.model_entry_id` 会在 provider call 前绑定唯一 entry。 |
| `src/tokenshare/experiments/run_lean_ai_benchmark.py` | Lean calibration CLI | 默认 scripted，真实 transport opt-in；不是新论文入口。 |
| `src/tokenshare/experiments/run_all.py` | regression CLI | 默认运行旧 Experiment 1-4，可 opt-in AI profile；不是新论文入口。 |
| `src/tokenshare/experiments/run_paper_experiments.py` | feat-011 Task 1 / 3×3 plan gate / Task 10 endpoint cohort CLI evidence | 新论文 CLI 骨架；`--plan-only` 写 `suite_manifest.json`、`run_budget.json` 和 `lean_3x3_matrix.json`，并默认加载 `benchmarks/paper/lean_lemma_graph_catalog.v1.jsonl` 参与 matrix/cell planning。非 plan-only 缺 `--real-transport` 时 blocked exit 1，缺预算 approval / digest 不匹配时 blocked exit 2。Task 10 review hardening 起正式 CLI 只支持 `--model-cohort-file`、`--model-entry-map` 和重复 `--provider-config provider=path` 的 Exp 5 plan-only preflight，写 `model_endpoint_cohort_plan.json`，并在 suite/budget manifest 中记录 `model_endpoint_cohort_preflight`；当 cohort/config/entry/key/smoke evidence 不完整时 suite status 为 structured blocked / ineligible、`provider_calls_made=0`、plan-only exit code 仍为 0，且同样输入下 `exp1` 不受 Exp5 cohort 缺口影响。`--model-policies` / `--strong-entry-id` / `--weak-entry-id` 已从正式 CLI 移除。 |
| `src/tokenshare/experiments/__init__.py` | Phase 8 / feat-011 exports | 导出旧 suites，并新增 paper model、catalog loader、condition planning helper、paper eligibility gate、factorization paper adapter、Lean paper adapter、paper fault injection / target descriptor、worker death harness 和 ablation support。legacy strong/weak/mixed model policy resolver 不再从 package public API 导出。 |
| `benchmarks/paper/factorization_catalog.v1.jsonl` | feat-011 Task 1 | Frozen factorization paper catalog，30 个 case，easy / medium / hard 各 10 个，含 oracle factors、candidate range、difficulty metadata、split params 和 source seed。 |
| `benchmarks/paper/lean_catalog.v1.jsonl` | feat-011 Task 1 | Frozen Lean paper catalog，30 个 case，easy / medium / hard 各 10 个，含 theorem payload、difficulty metadata、oracle proof preflight ref 和 environment digest。 |
| `benchmarks/paper/lean_lemma_graph_catalog.v1.jsonl` | paper_difficulty / topic family / Lean lemma-DAG preflight slice | Lean v2 fixture：3 个真实 `medium_lemma_dag` checker-backed golden cases，分别覆盖 `pure_logic`、`function_set`、`induction`。`pure_logic` case 为 root `R` 依赖 intermediate `Q`，并由 `P`、`P -> Q`、`Q -> R` 三个 leaf/sublemma proof units 支撑；`function_set` case 为本地 predicate set `D/E/F` 的 subset chain；`induction` case 为 Nat predicate induction 得到 `∀ n, P n` 后经 `P -> Q -> R` 链推出 root。每个 passed case 都含 fixed oracle package ref、真实 environment digest、DAG metadata、node payloads、per-node oracle proof source、expected AI unit count、topic/provenance/proof assembly metadata。另有 1 个 `hard_frontier` case 无 oracle proof package，显式 `preflight_status=structured_blocked`、`oracle_proof_package_ref=null`、`topic_family=pure_logic`、`proof_assembly_shape=structured_blocked_no_oracle_frontier_stress.v1`，用于验证 hard/frontier stress 不能伪造成 checker success。当前 fixture 只证明题库和 oracle package 可被 checker preflight，不代表 Lean plugin 已能执行递归 DAG split/merge。 |
| `benchmarks/paper/model_comparison_cohort.v1.json` | feat-011 Task 10 frozen model endpoint cohort | Frozen Experiment 5 cohort snapshot，`cohort_id=tokenshare.paper.model_endpoint_cohort.v1`，恰好包含 GLM-5.2/SiliconFlow、Qwen3.6-27B/SiliconFlow 和 GPT-5.6-Sol-high/OpenAI 三个 member；文件内保存 Artificial Analysis v4.1 背景分数、provider/model/reasoning provenance、source URLs 和 non-routing boundary。loader digest 当前为 `sha256:6a8745c4fdd419f616d540a92bb899fedf68f8c8bf110061680f054876aa2e02`。 |

## 2. Test Map

| 测试文件 | 覆盖内容 |
|---|---|
| `tests/experiments/test_phase8_models.py` | `SimulationProfile` digest、`ExperimentRun` run_id/status、blocked reason 必填。 |
| `tests/experiments/test_phase8_runner_reports.py` | 单 case runner：factorization semiprime report/metrics/CSV、Lean direct proof ready path、Lean decomposition/merge ready path、可注入 Lean blocked gate、默认真实 Lean preflight 缺 toolchain blocked、failure injection canonical pollution=0、ablation expected degradation、AI API usage/cost 从 submission artifact 复算。 |
| `tests/experiments/test_phase8_default_suite.py` | 默认 Experiment 1-4 case 矩阵和 suite 汇总；Experiment 4 包含 factorization semiprime passed、Lean direct proof passed、Lean decomposition/merge passed，均带真实 checker evidence；2026-07-02 覆盖 suite-level `phase8_experiment_settings.json`。 |
| `tests/experiments/test_phase8_simulation.py` | `SimulationWrapper` 五类 fault 和五个 ablation mode 决策记录。 |
| `tests/experiments/test_run_all_cli.py` | CLI 入口运行默认 suite、写 `phase8_suite_report.json`、包含 Lean direct proof / decomposition merge run manifest。 |
| `tests/experiments/test_ai_profile_suite.py` | AI profile suite 输出 schema、raw / parsed / parse failure refs、provider/model、usage、cost、latency、parser success、deterministic vs ai_api 对比、独立 CLI 和 `run_all --run-ai-profile` opt-in 接线；2026-06-30 新增 token budget、no-factor-only false success、strict arithmetic model filter 和 raw-only real-transport injection 回归；2026-07-02 覆盖 `ai_profile_settings.json` 和 deterministic baseline artifact root 持久化。 |
| `tests/experiments/test_factorization_500_ai.py` | direct 500 factorization benchmark：验证 500 个大整数输入生成、oracle 校验、direct answer 数学校验、scripted transport 下准确率报告 / settings / progress / JSONL / CSV 输出、secret 不落 settings、CLI 接线，以及 `entry_id` / `worker_count` 记录。 |
| `tests/experiments/test_lean_ai_benchmark.py` | Lean AI benchmark catalog 恰好 50 个任务，覆盖 25 个 conjunction / 25 个 iff payload、scripted suite 输出 settings/catalog/JSONL/CSV/report、以及 CLI `run_lean_ai_benchmark` 接线。 |
| `tests/experiments/test_paper_models.py` | Paper schema / digest / enum serialization：condition digest 稳定、required real transport / paper eligibility 控制字段、suite / experiment / condition / run / task / attempt / budget result 输出 schema，并覆盖 per-attempt provider attempt index、start/end time、prompt/completion token breakdown。2026-07-15 起覆盖 `paper_difficulty` 序列化和 condition digest 差异；schema retrofit 后覆盖 condition/task 的 topic/provenance/proof assembly 字段序列化、topic/construction rule 改变 digest，以及非法 Lean `topic_family` 拒绝。2026-07-16 G 影响面复验覆盖 `PaperAttemptResult` 新增 graph metadata 字段仍可序列化。 |
| `tests/experiments/test_paper_catalog.py` | Paper catalog loader：验证 30+30 frozen catalog、三档难度分布、catalog digest 稳定、factorization oracle/range 约束、本地 Lean checker-backed preflight summary，并覆盖 duplicate case、坏 factorization oracle、坏 embedded Lean oracle proof 和 Lean case environment digest drift 拒绝。2026-07-15 起覆盖 Lean v1 全部映射 simple、v2 lemma-DAG fixture 加载、v2 medium 缺 dependency_edges/oracle/environment 拒绝、hard_frontier no-oracle 不能 passed、以及 budget 使用 `expected_ai_unit_count`。schema retrofit 后覆盖 v2 row 缺 `topic_family` / `construction_rule_id` / `oracle_package_group` / `proof_assembly_shape` 拒绝、manifest `topic_family_counts` / `paper_difficulty_topic_family_counts`、`cases_for()` 按 paper difficulty + topic family 过滤、以及不把 shallow v1 legacy medium 混入 `medium_lemma_dag`。2026-07-16 plan gate 增加分布回归：simple 只来自 shallow v1 pure_logic，medium 三个 topic family 各一个 checker-backed case，hard/frontier no-oracle row 保持 `structured_blocked` 且 `oracle_proof_package_ref=null`。 |
| `tests/experiments/test_lean_lemma_graph_catalog.py` | Lean medium lemma-DAG catalog/preflight：验证 medium fixture 不再是 `P /\ Q` / `P ∧ Q` / `P ↔ Q` shallow variant，DAG depth >= 2 且包含 root/intermediate/leaf nodes、leaf_count >= 2、dependency_edges 无环、每个 node 有 statement/theorem payload/node kind/depth、oracle package file hash 匹配、node proof sources 覆盖所有 node、environment_digest 匹配当前 checker environment、`preflight_status=passed` 对应真实 checker-backed summary，并覆盖 environment digest mismatch、oracle package hash mismatch 和坏 node proof 被拒绝。schema retrofit 后覆盖 medium row 显式 `topic_family=pure_logic` / stable construction/oracle/proof-assembly IDs、hard_frontier no-oracle 仍 structured_blocked，以及 unsupported `proof_assembly_shape` 拒绝。2026-07-15 本轮新增覆盖 `function_set` / `induction` medium golden case 存在、preflight passed、node proof sources 覆盖所有 node、topic counts 和 medium topic-family counts。 |
| `tests/experiments/test_paper_real_ai_gate.py` | Paper eligibility gate：验证完整真实 provider evidence、transport evidence ref、secret scan report ref 和 artifact manifest inventory 才能 `paper_eligible=true`；scripted/fake/deterministic/mock transport、伪造 refs 无 inventory、缺 raw/usage/request/provenance/parsed-or-parse-failure evidence、缺 provider attempt 字段、secret scan failure 和缺 provider attempt 均派生结构化 ineligible reasons。 |
| `tests/experiments/test_factorization_paper_adapter.py` | Factorization paper adapter：验证 paper catalog case 先由插件 deterministic range split，所有 range children 通过 `AIAPIExecutor` / provider transport 生成 raw/parsed/provenance/usage evidence，再经插件 verifier 后 merge 为 root prime factorization；同时覆盖 false-negative range candidate 被 verifier 拒绝后阻止 merge、custom fake transport 或 `UrlLibSiliconFlowTransport` subclass 不能标成 real transport、parse/provider failure 的 root stage/kind 分类，以及 secret scan 未正式接入前保持 paper-ineligible。2026-07-16 review hardening 增加 OpenAI request artifact 断言和 registry matching 断言，确认 `ExecutionRequest.capability_snapshot` / `hard_requirements` 与 attempt/provider/usage 一致。 |
| `tests/experiments/test_lean_paper_adapter.py` | Lean paper adapter：v1 测试继续验证 paper catalog case 先由 Lean split helper 生成 split certificate，所有 child proof units 通过 `AIAPIExecutor` / provider transport 生成 raw/parsed/provenance/usage evidence，再经 Lean parser、child checker、proof artifact、environment digest 和 merge/root recheck 后形成 root result；同时覆盖 checker rejection 阻止 merge、parse/provider failure 的 root stage/kind 分类、custom fake transport 或 `UrlLibSiliconFlowTransport` subclass 不能标成 real transport，以及 secret scan 未正式接入前保持 paper-ineligible。2026-07-16 G 新增 v2 tests：pure_logic `lean_v2_medium_lemma_dag_01` 按 every lemma node 走 AIAPIExecutor / raw / parsed / `CHILD_PROOF` checker / assembled root merge；function_set 和 induction medium cases 最小回归；attempt / task / run evidence 带 topic/provenance/graph metadata；node checker rejection、parse failure、provider failure 不生成 accepted root；hard_frontier no-oracle structured blocked 且 provider calls 为 0；v1 `expected_split_kind in {"conjunction","iff"}` 不成为 v2 限制。2026-07-16 review hardening 增加 OpenAI simple child 与 v2 lemma-node request artifact 断言，并保留 SiliconFlow 路径一致性断言。 |
| `tests/experiments/test_paper_faults.py` | Paper fault injection：验证 deterministic seed target selection、graph/slot/range target descriptor metadata preservation、raw output 已持久化门槛、`false_positive` parsed candidate mutation、`false_negative` found candidate/proof suppression、没有 found candidate/proof 时拒绝 false-negative 注入、`no_return` lease expiry/retry record、`late_submission` deadline 后拒绝且不污染 canonical、`executor_error` controlled error record，以及每条 fault record 同时引用 original / mutated artifact refs。 |
| `tests/experiments/test_paper_workers.py` | Paper worker death harness：验证 multi-level generic dependency DAG、独立 worker process pid、25% / 50% / 75% kill point、coordinator 存活并等待 lease expiry、replacement worker 新 attempt、worker death record artifact、无 canonical pollution、synthetic provider token 为 0，以及缺失 dependency / cycle / inconsistent unit depth 拒绝。 |
| `tests/experiments/test_paper_ablation.py` | Paper ablation support：验证 Experiment 4 condition expansion 覆盖 2 domains × 3 difficulties × 6 ablation modes，plan-only budget 使用固定 5-task batch，并验证当前 shallow Lean v1 的 `lean_proof` medium / hard 条件不会被要求 paper-eligible；2026-07-15 起同时验证 expanded condition 的 `paper_difficulty`：factorization 为 easy/medium/hard，当前 Lean v1 为 simple。验证 ablation profile 只在实验边界关闭机制且拒绝安全 invariant override；验证 generic multi-level dependency graph 的 AI unit coverage gate 要求 manifest-verified 完整 artifact refs，拒绝 artifact-id-only fabricated refs 和 tampered graph digest；验证 canonical pollution、premature merge、blocked root、settlement blocked 和 slot integrity violation 均从带 event/artifact refs 的 evidence records 复算，clean records 也不能缺 refs，且非 bool metric flags 会被拒绝。 |
| `tests/experiments/test_paper_budget.py` | Paper budget guard：验证 plan-only budget 估算、非 plan-only approval digest 必填、digest mismatch 拒绝和条件 expansion 的成本统计。schema retrofit 后覆盖 Lean `medium_lemma_dag` budget 使用 v2 `expected_ai_unit_count`、不选择 shallow v1 legacy `difficulty=medium`，以及缺 topic-family cell 时抛 `insufficient catalog` 不 fallback。 |
| `tests/experiments/test_paper_model_policy.py` | feat-011 Task 10 review regression：验证正式 Experiment 5 fixed-entry endpoint cohort loader、entry map、provider config map 和 `build_model_endpoint_cohort_preflight()`；正确 member ID 但错误模型/错误 provider、GPT reasoning 非 high、entry-map cohort id mismatch、缺失或不匹配 smoke evidence、多出第四 member、缺任一 member 均 structured blocked，且 `provider_calls_made=0`；`PaperExperimentCondition.model_policy` 只接受 `fixed_entry` 并拒绝 `strong_only` / `weak_only` / `mixed`，正式 CLI 也不接受 `--model-policies`；`paper_runner.expand_plan_conditions(... exp5 ...)` 只从 planned preflight 展开 3 models × 2 domains × 3 difficulties = 18 个单 repeat conditions，记录 selected `model_entry_id`、member/provider/model/reasoning metadata，blocked cohort 不生成额外 condition；CLI plan-only `--model-cohort-file` / `--model-entry-map` / `--provider-config` 写 `model_endpoint_cohort_plan.json` / suite / budget evidence 且 provider calls 为 0；同一 incomplete cohort 输入下 `exp1` plan-only 仍成功且不携带 Exp5 cohort preflight；adapter identity 测试确认 `condition.model_entry_id` 会绑定唯一 entry。 |
| `tests/experiments/test_run_paper_experiments_cli.py` | Paper CLI 骨架：验证 plan-only 输出 `suite_manifest.json` / `run_budget.json`、缺 real transport blocked exit 1、缺 budget approval blocked exit 2。2026-07-16 起覆盖 plan-only 输出 `lean_3x3_matrix.json`、matrix cell count 为 9、blocked/insufficient catalog cell 不伪装可执行、shallow v1 不计入 medium/hard、expected AI unit count 按 topic_family 汇总、hard_frontier no-oracle 只 structured_blocked 且 `paper_eligible_possible=false`、provider calls 仍为 0；Task 10 review hardening 后正式 Exp5 CLI coverage 移到 `test_paper_model_policy.py`，旧 `--model-policies` / `--strong-entry-id` / `--weak-entry-id` 不再属于正式 CLI。 |
| `tests/plugins/lean_proof/test_lean_prompt_and_parse_policy.py` | 2026-07-02 追加 Lean proof candidate prompt contract：要求模型返回 exact `proof_candidate_id`、proof body only，不得输出 import / namespace / theorem / markdown / prose。 |

## 3. Boundary Notes

- Phase 8 只新增 `tokenshare.experiments` 层；未修改 `tokenshare.core`、`ProtocolEngine`、factorization verifier / merge policy、Phase 7 executor 或 storage authority。
- Factorization 实验 adapter 复用插件公开 fixture helper，不复制 divisibility verifier 或 merge policy 领域逻辑。
- Failure injection / ablation 当前是实验 wrapper 报告层语义，用于论文退化/隔离指标；不把错误路径改成 protocol core 默认行为。
- Metrics 从 JSONL events 和 artifact manifest / content digest 复算 evidence；不信任脚本内联成功布尔值作为唯一证据。
- Lean adapter 默认使用真实 preflight、checker logs、proof artifacts 和 `EnvironmentRef` 声明 Experiment 4 Lean 侧通过；如果这些 evidence 缺失，只能通过 blocked / pending path 报告，不能使用 `lean_stub` 或 synthetic evidence。
- 人类可通过 `conda run -n tokenshare python -m tokenshare.experiments.run_all --output-root outputs/experiments --seed 1` 运行旧默认 Experiment 1-4 regression；该入口不是新论文实验入口。
- 人类可通过 `conda run -n tokenshare python -m tokenshare.experiments.run_ai_profile --output-root outputs/experiments/ai_profile --seed 1` 单独运行 AI profile suite；默认 fake transport 不联网、不读取 secret，但仍持久化真实 `AIAPIExecutor` 输出 artifacts 和 submission event。
- 人类可通过 `--real-transport` 运行真实 AI profile；strict arithmetic profile 会基于模型适配诊断过滤已知不适配的 Qwen 条目，raw-only failure injection 仍保持受控 scripted stimulus，因此该 suite 同时可比较真实模型正常 JSON 输出和 parser failure 隔离。
- 人类可通过 `conda run -n tokenshare python -m tokenshare.experiments.run_factorization_500_ai --output-root outputs/experiments/factorization_500_ai_real_glm_seed1_count500_workers10_retry_20260702 --count 500 --seed 1 --real-transport --entry-id glm_5_2__sf_key_1 --max-provider-attempts 1 --max-tokens 256 --timeout-seconds 20 --worker-count 10` 运行 direct 500-number factorization benchmark；准确率分母为所有 attempted 输入，调用错误和数学校验失败均计入失败。
- 人类可通过 `conda run -n tokenshare python -m tokenshare.experiments.run_lean_ai_benchmark --output-root outputs/experiments/lean_ai_50 --count 50 --seed 1` 运行 Lean AI 50 benchmark；默认 scripted transport 不联网，真实 transport 必须显式加 `--real-transport`，并且不会把 API key 写入 event、artifact、SQLite、日志或 config digest。
- Lean AI 50 benchmark 只声明当前 Lean helper 已验证支持的浅层切片：`P ∧ Q` 和 `P ↔ Q`；不得用其 scripted 100% 结果替代新设计的三档难度真实 API 实验。
- `run_all --run-ai-profile` 只是 regression/calibration 报告；本地 config 存在但没有可用 API key 时使用 fake config 的行为，正是它不能成为新论文入口的原因。
- feat-011 Task 1 的 `budget / plan-only / approve-budget-digest` 是实验层成本安全阀，不是协议核心、线上 worker 或生产支付能力。
- feat-011 Task 2 的 Lean catalog preflight 已使用本地 Lean checker 验证 embedded oracle proof，因此 manifest 可记录 `lean_preflight_status=passed`；但这仍只证明 catalog theorem 本身为真，不等于真实 AI 已生成 proof candidate，也不等于 paper Lean adapter / hard case pilot 已完成。
- feat-011 Lean medium lemma-DAG preflight asset 已使用 fixed oracle package + 本地 Lean checker 验证每个 node proof，manifest 可记录 `lean_lemma_graph_preflight_summary.status=passed`；但这只证明题库/oracle package 有效，不等于 paper adapter 已执行 recursive DAG，也不等于 Lean plugin 已支持 recursive split/merge。
- feat-011 Task 2 的 paper eligibility gate 是实验层派生规则：它读取 `run_evidence`、transport evidence、secret scan report、artifact manifest inventory 和 attempt evidence 来判定 `paper_eligible`；不能写入 `tokenshare.core`，也不能替代后续 adapter/report 对真实 raw output、parsed output、provider provenance、usage、latency、token breakdown 和 cost artifact 的持久化。
- feat-011 Task 4 的 factorization paper adapter 只在实验层编排现有能力：split / parser / verifier / merge authority 仍在 `tokenshare.plugins.factorization`，provider evidence 仍由 `AIAPIExecutor` 和 `ArtifactStore` 持久化；测试用 scripted transport、任意自定义 fake transport 和 `UrlLibSiliconFlowTransport` subclass 都不能被标成 paper-eligible real run。当前第一版不实现 early success、sibling pruning、composite cofactor 递归 resolution、post-AI fault mutation、worker scaling runner 或正式 secret scan wiring。
- feat-011 Task 5 的 Lean paper adapter 只在实验层编排现有能力：split certificate、proof parser、child checker、merge/root recheck authority 仍在 `tokenshare.plugins.lean_proof`，provider evidence 仍由 `AIAPIExecutor` 和 `ArtifactStore` 持久化；测试用 scripted transport、任意自定义 fake transport 和 `UrlLibSiliconFlowTransport` subclass 都不能被标成 paper-eligible real run。当前第一版不实现 post-AI fault mutation、worker scaling runner、paper report/metrics、formal secret scan wiring 或真实 API pilot。
- feat-011 Task 7 的 paper fault injection 仍是实验层 wrapper 能力：它只在 raw / parsed evidence 已落盘后写 mutation artifact 和 `FaultInjectionRecord`，不改 `AIAPIExecutor` transport、parser、插件 verifier/checker、协议 canonical 默认语义或 token usage 统计。fault target schema 面向 generic AI unit / attempt / slot / artifact ref，可携带 Lean lemma-DAG 可选字段和 factorization range metadata，不绑定两个 child、`P ∧ Q` / `P ↔ Q` 或单层 child proof。`no_return`、`executor_error` 等恢复类 fault 只记录需要 replacement / retry；真正恢复 attempt 必须由后续 runner 再次真实调用 API，不得复用被故障注入丢弃或变换后的候选假装恢复。
- feat-011 Task 8 的 paper worker death harness 仍是实验层能力：它通过独立 subprocess 和现有 `LeaseManager` 纯规则生成 worker death / lease expiry / replacement attempt 证据，不写入协议事件流，也不把 synthetic replacement 当成真实 provider attempt。正式 Experiment 3 runner 仍必须在恢复 attempt 需要 AI 候选时重新调用真实 API。记录和测试面向 generic AI unit / attempt / dependency graph，后续 medium lemma-DAG catalog 可复用。
- feat-011 Task 9 的 paper ablation support 仍是实验层能力：`NO_PARSER_POLICY`、`NO_VERIFICATION`、`NO_REQUEUE`、`NO_MERGE_GATE`、`NO_SLOT_INTEGRITY` 只作为实验 profile / condition 变量，不改变系统默认 parser、verifier/checker、requeue、merge gate、slot integrity 或 settlement 行为。ablation 前置门槛是 generic dependency graph 中每个 AI unit 已有真实 provider attempt evidence，且相关 request / raw / provenance / usage / parsed-or-parse-failure artifact refs 必须能从 manifest inventory 校验；正式 runner 后续执行恢复或替代 attempt 时仍必须再次真实调用 API。当前 summary 只聚合 event/artifact-backed evidence records，不等于正式 Experiment 4 ablation table 已生成。当前 shallow Lean v1 的 medium / hard ablation 条件只能作为 downgraded / non-paper-eligible planning 条件，不能支撑正式 Lean medium/hard 主张。

## 4. 新论文实验实现缺口

已完成第一切片、Task 2 gate、Task 4 factorization paper adapter、Task 5 Lean paper adapter、Task 7 post-AI fault injection module、Task 8 worker death harness、Task 9 ablation support、2026-07-15 `paper_difficulty` / Lean v2 loader / medium lemma-DAG preflight asset / topic schema retrofit slice，以及 2026-07-16 Lean plugin proof-file assembly/root recheck、实验层 v2 adapter DAG execution slice和 Lean 3×3 plan-only / blocked-aware gate：`paper_models.py`、`paper_catalog.py`、`paper_budget.py`、`paper_runner.py`、`run_paper_experiments.py` 的最小 plan-only / gated skeleton，factorization / Lean 两个 30-task 三档难度 frozen catalog，本地 Lean checker-backed catalog preflight，基于 `run_evidence` / artifact manifest inventory 的真实 API paper eligibility 派生规则，factorization range-child AI executor / parser / verifier / merge adapter 第一版，Lean v1 split-helper / AI proof candidate / parser / checker / merge-root-recheck adapter 路径，Lean v2 medium lemma-DAG catalog/certificate-driven node AI proof unit / checker / assembled root merge 路径，Lean 3×3 matrix cell 级 plan manifest，fault target deterministic selection / mutation artifact / `FaultInjectionRecord` 第一版，generic dependency graph / worker death / lease expiry / replacement attempt record 第一版，ablation mode/profile / manifest-verified generic attempt coverage / evidence-derived summary 第一版，以及 checker-backed `lean_lemma_graph_catalog.v1.jsonl` medium oracle package preflight / hard-frontier blocked fixture / topic-family manifest / budget selection support。该阶段仍只保证 schema、catalog、digest、budget approval、CLI safety gate、eligibility evidence rule、factorization adapter 单 case 路径、Lean v1 adapter 单 case 路径、Lean v2 medium lemma-DAG adapter catalog-case 路径、Lean 3×3 plan-only blocked-aware 输出、post-AI fault record/mutation 路径、worker death harness 单 case 路径、ablation support、v2 catalog planning support、topic metadata 和 medium oracle package preflight 可验证。

仍未完成的新论文实验缺口：fault-aware runner integration / 正式故障矩阵执行、worker scaling condition expansion、model policy、paper metrics/report/CSV/secret scan wiring、full CLI/pilot、Experiment 1-5 正式 real API runs、正式 Experiment 4 ablation table、论文表格/图、3×3 批量题库扩展和 hard/frontier oracle-backed expansion。Lean 3×3 topic matrix 仍未填满：`pure_logic` / `function_set` / `induction` 目前各只有 1 个 checker-backed `medium_lemma_dag` golden case；`pure_logic` 另有 shallow v1 simple pool 和 1 个 no-oracle hard/frontier structured-blocked row；`function_set` / `induction` 的 simple 与 hard cells 仍 blocked。所有 v2 medium cases 现在能在 adapter 单 case scripted/test transport 下跑通 artifact/checker/merge evidence path，但尚未进入正式 real API runner / paper-eligible matrix。详细字段、实验矩阵、文件职责和验证命令只以 `tokenshare_latest_real_plugin_experiment_design.md` 为准。
- Phase 8 evidence check 不是完整 replay / audit engine；完整 state replay、audit replay 和 no-double-settlement 已从当前剩余必做路径中延后，不作为 2026-07-12 后开始真实 AI 论文实验前的 active feature。

## 5. Verification Evidence

- 2026-07-16 G Lean paper adapter v2 medium lemma-DAG execution：本轮只升级实验层 adapter，不修改 `tokenshare.core`、`SplitRules.lean`、`AIAPIExecutor`、catalog manifest 或 runner full 3×3 matrix，也未启动正式真实 API run。v2 path 根据 schema `tokenshare.paper_lean_lemma_graph_case.v1` dispatch，从 catalog deterministic data 构造 `LeanLemmaGraphCertificate` artifact，调用 `build_lean_split_plan()`，对每个 lemma node 建 `lean_proof_lemma_node` AI request，metadata 记录 `lemma_node_id`、`slot_key`、`dependency_path`、`paper_difficulty`、`topic_family`、`construction_rule_id`、`oracle_package_group`、`proof_assembly_shape`；每个 node 经 `AIAPIExecutor` raw / parsed artifact 和真实 Lean `CHILD_PROOF` checker evidence 后，accepted proofs 交给 `merge_lean_lemma_graph_proofs()` assembled proof + `MERGE_PROOF` root recheck。hard/frontier no-oracle 只 structured blocked，0 provider calls，不 checker success。RED `tests\experiments\test_lean_paper_adapter.py -q` 失败 `7 failed, 5 passed`，失败点为旧 adapter 拒绝 v2 schema；GREEN 同命令通过 `12 passed in 52.44s`。Impact `tests\experiments\test_lean_lemma_graph_catalog.py tests\experiments\test_paper_catalog.py tests\plugins\lean_proof -q` 通过 `111 passed in 96.89s`；`tests\experiments\test_paper_models.py -q` 通过 `9 passed in 0.24s`；`conda run --no-capture-output -n tokenshare python -m compileall -x "reference_repos" src tests` 退出码 0；`git diff --check` 退出码 0，仅有既有 LF/CRLF warning。本轮未联网。
- 2026-07-16 Lean 3×3 plan-only / blocked-aware gate：本轮只实现 plan gate，不启动正式批量真实 API run，不修改 Lean plugin、proof assembly、AI executor transport 或 Task 7/8/9 内核。`paper_runner.py` 新增 `build_lean_3x3_matrix_plan()`，固定输出 9 个 Lean matrix cells；`run_paper_experiments.py --plan-only` 默认加载 `lean_lemma_graph_catalog.v1.jsonl`，写 `lean_3x3_matrix.json`，并把同一 matrix 嵌入 `run_budget.json` 的 `quota_preflight.lean_3x3_matrix`；`paper_budget.py` 将 matrix 纳入 budget digest 且 `provider_calls_made=0`。当前 matrix 中只有 `simple/pure_logic` 的 30 个 shallow v1 checker-backed cases 达到 target 10；`medium_lemma_dag` 三个 topic family 各 1 个 checker-backed golden case，均 `blocked_reason=insufficient_catalog`；`hard_frontier/pure_logic` 只有 no-oracle `structured_blocked` frontier stress row，checker-backed available count 为 0 且 `paper_eligible_possible=false`。RED `tests\experiments\test_paper_catalog.py tests\experiments\test_run_paper_experiments_cli.py -q` 失败 2 项，失败点为缺 `lean_3x3_matrix.json`；GREEN 同命令通过 `29 passed in 44.16s`。状态同步后指定验证通过：`tests\experiments\test_paper_models.py tests\experiments\test_paper_catalog.py tests\experiments\test_run_paper_experiments_cli.py -q` 为 `38 passed in 51.16s`；`tests\experiments\test_lean_paper_adapter.py -q` 为 `12 passed in 57.46s`；`conda run -n tokenshare python -m compileall -x "reference_repos" src tests` 退出码 0；`git diff --check` 退出码 0，仅有既有 LF/CRLF warning；最终完整 `powershell -ExecutionPolicy Bypass -File .\init.ps1` 通过，pytest collected 525 items，结果 `524 passed, 1 skipped in 237.39s`。本轮未联网。
- 2026-07-15 feat-011 first topic-family golden cases：启动基线 `powershell -ExecutionPolicy Bypass -File .\init.ps1` 通过，输出 `python-json-sqlite-ok`、`harness-files-ok`，pytest collected 484 items，结果 `483 passed, 1 skipped in 342.49s`。本轮新增 `function_set / medium_lemma_dag` case `lean_v2_medium_function_set_dx_subset_chain_01` 和 `induction / medium_lemma_dag` case `lean_v2_medium_induction_nat_predicate_chain_01`，保留既有 `pure_logic / medium_lemma_dag` case `lean_v2_medium_lemma_dag_01` 和 hard/frontier no-oracle structured blocked row。RED targeted `tests\experiments\test_lean_lemma_graph_catalog.py tests\experiments\test_paper_catalog.py -q` 失败 11 项，失败点集中在缺 `function_set` / `induction` medium rows、topic counts 和 topic-filtered selection；GREEN 同一命令通过，结果 `33 passed in 89.91s`。Manifest summary：catalog digest `sha256:f7ce4576bc12a657c99d582cd78e5db9b41055a098b319408195fd3a055dc7d9`，`topic_family_counts={'pure_logic': 32, 'function_set': 1, 'induction': 1}`，`medium_lemma_dag` topic counts 为三类各 1；`lean_lemma_graph_preflight_summary.status=passed`，`accepted_case_count=3`，`blocked_case_count=1`，accepted node counts 为 pure_logic 5、function_set 4、induction 5；oracle package hash `sha256:4af791228bd24f4ea6d92fed511be8eee4f29d1ba8db5c6102968e743a8047ca`；environment digest `sha256:cdc9de4cb0a8407f17ddd7cf423a3fb5640a4560c3b55bd6a70116f5a14de731`。Budget regression `tests\experiments\test_paper_budget.py -q` 通过，结果 `5 passed in 54.06s`；Lean plugin impact `tests\plugins\lean_proof -q` 通过，结果 `54 passed in 73.41s`；`conda run -n tokenshare python -m compileall -x "reference_repos" src tests` 退出码 0。最终完整 `powershell -ExecutionPolicy Bypass -File .\init.ps1` 通过，pytest collected 491 items，结果 `490 passed, 1 skipped in 330.70s`。本轮未联网，未修改 `tokenshare.core`、`AIAPIExecutor`、factorization plugin、`SplitRules.lean`、Lean plugin split/merge、proof-file assembly、paper adapter DAG execution 或 runner full 3×3 matrix。
- 2026-07-15 feat-011 Lean topic schema retrofit / budget case-selection fix：启动基线 `powershell -ExecutionPolicy Bypass -File .\init.ps1` 失败 1 项，现有 `test_budget_estimation_uses_lean_v2_expected_ai_unit_count` 显示 budget 对 `paper_difficulty=medium_lemma_dag` 只按 legacy `difficulty=medium` 选题，实际混入 10 个 shallow v1 case 得到 11 root runs。RED targeted `tests\experiments\test_paper_models.py tests\experiments\test_paper_catalog.py tests\experiments\test_lean_lemma_graph_catalog.py tests\experiments\test_paper_budget.py -q` 失败 19 项，覆盖缺 topic/provenance fields、缺 topic-family manifest counts、`cases_for(topic_family=...)` 不支持、v2 catalog row 缺 metadata、unsupported proof assembly shape 未拒绝、budget 混入 shallow v1 medium 和缺 topic cell fallback。GREEN 同一目标命令通过，结果 `40 passed in 38.60s`。CLI / ablation impact `tests\experiments\test_run_paper_experiments_cli.py tests\experiments\test_paper_ablation.py -q` 通过，结果 `18 passed in 15.60s`。`conda run -n tokenshare python -m compileall -x "reference_repos" src tests` 退出码 0；`git diff --check` 退出码 0，仅有既有 LF/CRLF warning。本轮未联网，未修改 `tokenshare.core`、Lean plugin、`SplitRules.lean`、proof assembly 或 adapter DAG execution。
- 2026-07-15 feat-011 paper_difficulty schema / Lean v2 lemma-DAG catalog loader：启动基线 `powershell -ExecutionPolicy Bypass -File .\init.ps1` 先通过，输出 `python-json-sqlite-ok`、`harness-files-ok`，pytest collected 447 items，结果 `446 passed, 1 skipped in 204.64s`。RED targeted `tests\experiments\test_paper_models.py tests\experiments\test_paper_catalog.py -q` 失败 10 项，失败点是缺 `paper_difficulty`、缺 `paper_difficulty_counts`、`load_paper_catalogs()` 不支持 `lean_lemma_graph_path`。GREEN targeted 同命令通过，结果 `17 passed in 32.57s`。Paper budget / CLI / ablation impact `tests\experiments\test_paper_models.py tests\experiments\test_paper_catalog.py tests\experiments\test_paper_budget.py tests\experiments\test_run_paper_experiments_cli.py tests\experiments\test_paper_ablation.py -q` 通过，结果 `37 passed in 33.52s`。Experiment impact `tests\experiments -q` 通过，结果 `94 passed in 117.69s`。`conda run -n tokenshare python -m compileall -x "reference_repos" src tests` 退出码 0。本轮未联网，未修改 Lean plugin / `SplitRules.lean` / adapter DAG execution。
- 2026-07-15 feat-011 minimal real Lean medium lemma-DAG preflight asset：在不修改 Lean plugin / `SplitRules.lean` / adapter DAG execution 的前提下新增 `fixtures/lean_proof_project/TokenShare/LemmaGraphCases.lean`、`fixtures/lean_proof_project/TokenShare/LemmaGraphOracle.lean` 和 checker-backed medium row。TDD RED `tests\experiments\test_lean_lemma_graph_catalog.py -q` 首次失败 4 项，暴露 medium root 仍是 shallow `P /\ Q`、environment digest mismatch 未拒绝、oracle package hash mismatch 未拒绝、坏 node oracle proof 未拒绝；GREEN 后 `tests\experiments\test_lean_lemma_graph_catalog.py -q` 通过 `4 passed in 21.17s`，`tests\experiments\test_paper_catalog.py -q` 通过 `12 passed in 39.20s`，`tests\plugins\lean_proof -q` 通过 `45 passed in 34.59s`，`conda run --no-capture-output -n tokenshare python -m compileall -x "reference_repos" src tests` 退出码 0；完整 `powershell -ExecutionPolicy Bypass -File .\init.ps1` 通过，输出 `python-json-sqlite-ok`、`harness-files-ok`，pytest collected 458 items，结果 `457 passed, 1 skipped in 196.17s`。本轮未联网；medium oracle package preflight 只证明题库有效，不代表 recursive adapter execution 或 Lean plugin split/merge 已实现。
- 2026-07-14 feat-011 Task 1 paper model/catalog/budget/CLI skeleton：启动基线 `powershell -ExecutionPolicy Bypass -File .\init.ps1` 通过，输出 `python-json-sqlite-ok`、`harness-files-ok`，pytest collected 388 items，结果 `387 passed, 1 skipped in 175.44s`。RED targeted paper tests 首次失败于缺少 `tokenshare.experiments.paper_models`、`paper_catalog`、`paper_budget` 和 `run_paper_experiments`。代码审查后修复 Lean manifest overclaim 和 invalid domain/difficulty 静默预算问题。GREEN targeted `$env:PYTHONPATH='src'; conda run -n tokenshare python -m pytest tests\experiments\test_paper_models.py tests\experiments\test_paper_catalog.py tests\experiments\test_paper_budget.py tests\experiments\test_run_paper_experiments_cli.py -q` 最终通过，结果 `13 passed in 0.33s`。影响面 `$env:PYTHONPATH='src'; conda run -n tokenshare python -m pytest tests\experiments -q` 最终通过，结果 `41 passed in 68.27s`。`conda run -n tokenshare python -m compileall -x "reference_repos" .` 退出码 0。最终 `powershell -ExecutionPolicy Bypass -File .\init.ps1` 通过，输出 `python-json-sqlite-ok`、`harness-files-ok`，pytest collected 401 items，结果 `400 passed, 1 skipped in 138.50s`。
- 2026-07-14 feat-011 Task 2 real-AI paper eligibility gate / checker-backed catalog preflight：启动基线 `powershell -ExecutionPolicy Bypass -File .\init.ps1` 通过，输出 `python-json-sqlite-ok`、`harness-files-ok`，pytest collected 401 items，结果 `400 passed, 1 skipped in 167.07s`。RED catalog gate tests 先失败于 `lean_preflight_status` 仍为 `metadata_only` 且坏 embedded Lean proof 未被拒绝；实现过程中 targeted 一次失败于缺少 `_safe_artifact_id`，修复后 `tests\experiments\test_paper_catalog.py -q` 通过，结果 `5 passed in 20.05s`。RED real-AI gate test 失败于缺少 `evaluate_paper_eligibility`；实现 `PaperEligibilityReport` / `evaluate_paper_eligibility()` 后 `tests\experiments\test_paper_real_ai_gate.py -q` 通过，结果 `4 passed in 0.23s`。代码审查随后发现 gate 仍信任调用方 `real_transport` / `secret_scan_passed` 布尔参数且 per-attempt schema 缺少 provider attempt index 与 token breakdown；新增 RED 覆盖伪造 refs 无 inventory、缺 provider attempt index / prompt / completion token 证据后，修复为 `run_evidence` + artifact manifest inventory 派生。修复后 targeted `tests\experiments\test_paper_models.py tests\experiments\test_paper_real_ai_gate.py -q` 通过，结果 `9 passed in 0.24s`；合并 paper slice 通过，结果 `19 passed in 30.15s`；`conda run -n tokenshare python -m compileall -x "reference_repos" src tests` 退出码 0；影响面 `tests\experiments -q` 通过，结果 `47 passed in 96.98s`；最终完整 `powershell -ExecutionPolicy Bypass -File .\init.ps1` 通过，输出 `python-json-sqlite-ok`、`harness-files-ok`，pytest collected 407 items，结果 `406 passed, 1 skipped in 171.09s`。
- 2026-07-15 feat-011 Task 7 post-AI fault injection：启动基线 `powershell -ExecutionPolicy Bypass -File .\init.ps1` 通过，输出 `python-json-sqlite-ok`、`harness-files-ok`，pytest collected 418 items，结果 `417 passed, 1 skipped in 188.97s`。RED targeted `tests\experiments\test_paper_faults.py -q` 失败于 `ModuleNotFoundError: No module named 'tokenshare.experiments.paper_faults'`。GREEN targeted 同命令通过，结果 `7 passed in 0.41s`。Task 7 + paper gate + adapter impact `tests\experiments\test_paper_faults.py tests\experiments\test_paper_real_ai_gate.py tests\experiments\test_factorization_paper_adapter.py tests\experiments\test_lean_paper_adapter.py -q` 通过，结果 `22 passed in 25.04s`。`tests\experiments -q` 通过，结果 `65 passed in 103.72s`。`conda run -n tokenshare python -m compileall -x "reference_repos" src tests` 退出码 0。完整 `powershell -ExecutionPolicy Bypass -File .\init.ps1` 通过，输出 `python-json-sqlite-ok`、`harness-files-ok`，pytest collected 425 items，结果 `424 passed, 1 skipped in 165.67s`。
- 2026-07-15 feat-011 Task 7 strict self-review hardening：针对自查发现的 lemma-DAG target schema / graph-agnostic test / false-negative applicability 缺口，新增 `FaultTargetDescriptor`、`select_fault_target_descriptors()`、`target_context`、`target_selection_digest`、graph/slot/range metadata regression，以及 false-negative no-candidate 拒绝测试。RED targeted `tests\experiments\test_paper_faults.py -q` 先失败于无法导入 `FaultTargetDescriptor`；GREEN targeted 通过，结果 `9 passed in 0.37s`。Task 7 + gate + adapters + worker impact 通过，结果 `29 passed in 27.45s`。`conda run -n tokenshare python -m compileall -x "reference_repos" src tests` 退出码 0。`tests\experiments -q` 通过，结果 `72 passed in 109.07s`。危险模式扫描未发现 Task 7 代码依赖两个 child、`P ∧ Q`、`P ↔ Q`、left/right 或 conjunction/iff 结构。完整 `powershell -ExecutionPolicy Bypass -File .\init.ps1` 通过，输出 `python-json-sqlite-ok`、`harness-files-ok`，pytest collected 432 items，结果 `431 passed, 1 skipped in 180.78s`。
- 2026-07-15 feat-011 Task 8 worker death harness：启动基线 `powershell -ExecutionPolicy Bypass -File .\init.ps1` 通过，输出 `python-json-sqlite-ok`、`harness-files-ok`，pytest collected 425 items，结果 `424 passed, 1 skipped in 199.11s`。RED targeted `tests\experiments\test_paper_workers.py -q` 失败于 `ModuleNotFoundError: No module named 'tokenshare.experiments.paper_workers'`。GREEN targeted 同命令通过，结果 `5 passed in 0.82s`。Task 8 指定影响面 `tests\experiments\test_paper_workers.py tests\core tests\storage -q` 通过，结果 `104 passed in 10.87s`。实验层回归 `tests\experiments -q` 通过，结果 `72 passed in 111.21s`。`conda run -n tokenshare python -m compileall -x "reference_repos" src tests` 退出码 0。完整 `powershell -ExecutionPolicy Bypass -File .\init.ps1` 通过，输出 `python-json-sqlite-ok`、`harness-files-ok`，pytest collected 432 items，结果 `431 passed, 1 skipped in 184.32s`。
- 2026-07-15 feat-011 Task 9 ablation support：启动基线 `powershell -ExecutionPolicy Bypass -File .\init.ps1` 通过，输出 `python-json-sqlite-ok`、`harness-files-ok`，pytest collected 432 items，结果 `431 passed, 1 skipped in 180.69s`。RED targeted `tests\experiments\test_paper_ablation.py -q` 失败于 `ModuleNotFoundError: No module named 'tokenshare.experiments.paper_ablation'`。GREEN targeted 同命令通过，结果 `10 passed in 16.14s`；中性化 generic DAG unit id 后，相邻影响面 `tests\experiments\test_paper_ablation.py tests\experiments\test_paper_budget.py tests\experiments\test_run_paper_experiments_cli.py tests\experiments\test_paper_models.py -q` 通过，结果 `19 passed in 15.71s`。危险模式扫描未发现 Task 9 代码依赖两个 child、`P ∧ Q`、`P ↔ Q`、left/right 或 conjunction/iff；只命中通用 `difficulty=hard` 字段。`tests\experiments -q` 通过，结果 `82 passed in 108.62s`。`conda run -n tokenshare python -m compileall -x "reference_repos" src tests` 退出码 0。executor / factorization / Lean plugin impact `tests\executors tests\plugins\factorization tests\plugins\lean_proof -q` 通过，结果 `134 passed, 1 skipped in 33.82s`。完整 `powershell -ExecutionPolicy Bypass -File .\init.ps1` 通过，输出 `python-json-sqlite-ok`、`harness-files-ok`，pytest collected 442 items，结果 `441 passed, 1 skipped in 172.80s`。
- 2026-07-15 feat-011 Task 9 review hardening：补强 artifact inventory coverage、profile invariant、clean record refs 和 current Lean v1 medium/hard downgrade。新增回归后 RED targeted `tests\experiments\test_paper_ablation.py -q` 失败于 `_provider_attempt_reasons() got an unexpected keyword argument 'artifact_inventory'`；修复私有接口后同命令通过，结果 `13 passed in 15.87s`。Task 9 + budget/CLI/model impact `tests\experiments\test_paper_ablation.py tests\experiments\test_paper_budget.py tests\experiments\test_run_paper_experiments_cli.py tests\experiments\test_paper_models.py -q` 通过，结果 `22 passed in 15.99s`。`tests\experiments -q` 通过，结果 `85 passed in 115.53s`。`conda run -n tokenshare python -m compileall -x "reference_repos" src tests` 退出码 0。executor / factorization / Lean plugin impact `tests\executors tests\plugins\factorization tests\plugins\lean_proof -q` 通过，结果 `134 passed, 1 skipped in 36.07s`。`feature-list-json-ok`、状态文档 BOM 扫描、精确危险模式扫描和 `git diff --check` 通过；`git diff --check` 仅有既有 LF/CRLF warning。状态同步后完整 `powershell -ExecutionPolicy Bypass -File .\init.ps1` 通过，输出 `python-json-sqlite-ok`、`harness-files-ok`，pytest collected 445 items，结果 `444 passed, 1 skipped in 173.80s`。
- 2026-07-15 feat-011 Task 7/8/9 inspection：按用户要求复查 “Lean 复杂题库与递归 lemma-DAG 要求” 后修复 4 个问题：`NO_PARSER` 枚举漂移为权威 `NO_PARSER_POLICY`；ablation coverage 重算并校验 dependency graph digest；ablation metric flags 拒绝字符串布尔值；worker dependency graph 拒绝声明 depth 与实际 DAG depth 不一致。新增回归 RED targeted `tests\experiments\test_paper_workers.py tests\experiments\test_paper_ablation.py -q` 先失败 3 项，修复后同命令通过，结果 `20 passed in 16.27s`。Task 7/8/9 + budget/CLI/model impact 通过，结果 `38 passed in 17.26s`。`tests\experiments -q` 通过，结果 `87 passed in 107.31s`。`conda run -n tokenshare python -m compileall -x "reference_repos" src tests` 退出码 0。executor / factorization / Lean plugin impact `tests\executors tests\plugins\factorization tests\plugins\lean_proof -q` 通过，结果 `134 passed, 1 skipped in 33.90s`。feature-list JSON 解析、状态文档 BOM 扫描、精确危险模式扫描和 `git diff --check` 通过。最终 `powershell -ExecutionPolicy Bypass -File .\init.ps1` 通过，输出 `python-json-sqlite-ok`、`harness-files-ok`，pytest collected 447 items，结果 `446 passed, 1 skipped in 177.06s`。
- 2026-07-12 新论文实验设计同步：删除旧 Phase 8 实验 TDD 和旧 `.tex/.pdf` 权威，新增 `tokenshare_latest_real_plugin_experiment_design.md` 并把本文降级为实现/缺口 code map；AGENTS、README、导航、主 TDD、feature/progress/handoff 已同步。Markdown 结构、source-document paths、JSON、删除审计和 `git diff --check` 通过；更新后完整 `.\init.ps1` 通过，pytest collected 388 items，结果 `387 passed, 1 skipped in 146.87s`。
- 启动基线：`.\init.ps1` 通过，输出 `python-json-sqlite-ok`、`harness-files-ok`，pytest collected 301 items，结果 `300 passed, 1 skipped in 30.43s`。
- RED 1：`tests\experiments -q` 失败，原因是缺少 `tokenshare.experiments.models` 和 `tokenshare.experiments.adapters`。
- GREEN 1：实现 models / adapters / report / metrics / factorization adapter / Lean blocked adapter / runner 后，`tests\experiments -q` 通过，结果 `7 passed in 2.19s`。
- RED 2：新增 default suite 测试后失败，原因是缺少 `default_experiment_cases()` / `run_phase8_default_suite()`；修复后 `tests\experiments\test_phase8_default_suite.py -q` 通过。
- RED 3：suite summary 将 `inconclusive` 消融误计入 `passed_runs`；修复为单列 `inconclusive_runs` 后默认 suite 测试通过。
- RED 4：Experiment 4 默认矩阵缺 factorization semiprime lifecycle 和 Lean decomposition / merge gate；补齐后默认 suite 测试通过。
- RED 5：新增 `SimulationWrapper` 测试失败于缺少 `tokenshare.experiments.simulation`；实现后 `tests\experiments\test_phase8_simulation.py -q` 通过。
- RED 6：新增 AI API usage/cost metrics 测试后失败，原因是 `build_metrics()` 仍把 `ai_api_usage_cost` / `ai_api_executor_effect` 固定为 0，而不是从 `EXECUTION_SUBMISSION_RECORDED` 指向的 `ExecutionSubmission` artifact 复算。
- 第一版 targeted verification：`tests\experiments\test_phase8_runner_reports.py -q` 通过，结果 `5 passed in 2.11s`；`tests\experiments -q` 通过，结果 `10 passed in 13.89s`。
- Lean ready-path update targeted verification：Lean Phase 8 ready targeted tests 通过，结果 `3 passed in 6.83s`；`tests\experiments -q` 通过，结果 `12 passed in 25.10s`；combined Lean plugin/E2E/experiments suite 通过，结果 `55 passed in 65.42s`。
- 冲击面验证：`tests\test_phase6_factorization_flow.py tests\test_phase6_factorization_limitations.py tests\executors tests\test_phase7_ai_api_execution_flow.py -q` 通过，结果 `45 passed, 1 skipped in 5.38s`。
- Compile verification：`conda run -n tokenshare python -m compileall -x "reference_repos" .` 退出码 0。
- Whitespace check：`git diff --check` 退出码 0，仅有 LF/CRLF warning。
- 2026-06-29 `run_all` CLI 补丁：RED targeted test first failed with missing `tokenshare.experiments.run_all`; GREEN `tests\experiments\test_run_all_cli.py -q` passed with `1 passed in 16.03s`; wider `tests\experiments -q` passed with `14 passed in 40.51s`; actual CLI `$env:PYTHONPATH='src'; conda run -n tokenshare python -m tokenshare.experiments.run_all --output-root outputs\experiments --seed 1` exited 0 and reported `total_runs=17`, `passed_runs=12`, `inconclusive_runs=5`, `blocked_runs=0`; final `.\init.ps1` passed with pytest collected 368 items, result `367 passed, 1 skipped in 115.85s`.
- 2026-06-30 AI profile suite 补强：RED 1 `tests\experiments\test_ai_profile_suite.py -q` first failed with `ModuleNotFoundError: No module named 'tokenshare.experiments.ai_profile'`; GREEN after adding `ai_profile.py` and `run_ai_profile.py` passed with `2 passed in 2.40s`。RED 2 `run_all --run-ai-profile` targeted test failed with unrecognized argument `--run-ai-profile`; GREEN after opt-in wiring passed with `tests\experiments\test_ai_profile_suite.py -q` = `3 passed in 20.15s`。最新验证待本轮最终完整验证记录为准。
- 2026-06-30 real AI strict parser 修复：RED 增量覆盖 token budget 仍为 512、no-factor-only partial output 被误判为 semiprime success、缺少 strict arithmetic model filter、raw-only failure injection 在 real-transport suite 中误走正常 real prompt。GREEN `tests\experiments\test_ai_profile_suite.py -q` 通过，结果 `7 passed in 20.94s`；影响面 `tests\executors tests\plugins\factorization tests\experiments tests\test_phase7_ai_api_execution_flow.py -q` 通过，结果 `111 passed, 1 skipped in 65.25s`；真实 transport `run_ai_profile --output-root outputs\experiments\ai_profile_real_fixed4 --seed 1 --real-transport` 退出码 0，`ai_api_semiprime_range_flow` passed，`parser_success_rate=1.0`，`final_correctness=true`，`prime_factors=["7","13"]`，`parse_failure_count=0`，`provider_attempt_count=4`，model `MiniMaxAI/MiniMax-M2.5`；`ai_api_parse_failure_raw_only` passed with `parse_failure_count=1`。
- 2026-07-02 论文实验 setting / result 重跑补强：RED `tests\experiments\test_phase8_default_suite.py tests\experiments\test_ai_profile_suite.py -q` 先失败于缺少 `settings_path`；GREEN after adding `phase8_experiment_settings.json` / `ai_profile_settings.json` passed with `8 passed in 42.25s`。随后发现 AI profile deterministic baseline artifact root 指向已删除临时目录；新增回归先失败，修复为持久化到 `deterministic_semiprime_range_flow/fixture_flow/artifacts` 后 targeted passed with `1 passed in 1.09s`。实验测试 `tests\experiments -q` 通过，结果 `21 passed in 72.89s`。清空旧 `outputs\experiments` 后重跑默认 suite：`run_all --output-root outputs\experiments --seed 1` 退出码 0，`total_runs=17`、`passed_runs=12`、`inconclusive_runs=5`、`failed_runs=0`、`blocked_runs=0`，写出 `phase8_experiment_settings.json`、`phase8_suite_report.json`、`phase8_suite_summary.csv`。真实 AI profile `run_ai_profile --output-root outputs\experiments\ai_profile_real --seed 1 --real-transport` 退出码 0，AI semiprime profile passed，`parser_success_rate=1.0`、`final_correctness=true`、`raw_output_count=4`、`parsed_output_count=4`、`parse_failure_count=0`、model `MiniMaxAI/MiniMax-M2.5`、`usage.total_tokens=7469`、`cost_estimate_total=0.0041459999999999995`、`latency_ms_total=45858`；raw-only profile preserved `parse_failure_count=1`。Secret scan over 390 output files checked one local secret value and passed with no leak。
- 2026-07-02 Lean AI 50 benchmark：RED `tests\experiments\test_lean_ai_benchmark.py -q` 先失败于缺少 `tokenshare.experiments.lean_ai_benchmark`；GREEN 后 targeted 通过，结果 `3 passed in 19.35s`。真实 API smoke 首轮暴露模型返回完整 theorem/namespace 而非 proof body、且 `proof_candidate_id` 不符合 parser contract；针对 prompt contract 新增回归先失败于缺少 `expected_proof_candidate_id`，加固后 `tests\plugins\lean_proof\test_lean_prompt_and_parse_policy.py -q` 通过，结果 `5 passed in 0.23s`。真实 transport smoke `run_lean_ai_benchmark --output-root outputs\experiments\lean_ai_50_real_smoke_fixed --count 1 --seed 1 --real-transport` 退出码 0，`attempted=1`、`passed=1`、`parsed_output_count=2`、`checker_accepted_count=2`、`merge_success_count=1`、`cost_estimate_total=0.004036774999999999`、`latency_ms_total=25137`。scripted full 50 `run_lean_ai_benchmark --output-root outputs\experiments\lean_ai_50_scripted_full --count 50 --seed 1` 退出码 0，`attempted=50`、`passed=50`、`raw_output_count=100`、`parsed_output_count=100`、`checker_accepted_count=100`、`merge_success_count=50`、`parser_success_rate=1.0`、`checker_success_rate=1.0`。
- 2026-07-02 direct 500 factorization benchmark：RED `tests\experiments\test_factorization_500_ai.py -q` 先失败于缺少 `tokenshare.experiments.factorization_500_ai`；GREEN 后 targeted 通过，最新结果 `4 passed in 0.61s`。影响范围验证 `tests\experiments\test_factorization_500_ai.py tests\experiments\test_ai_profile_suite.py tests\experiments\test_phase8_default_suite.py tests\experiments\test_phase8_runner_reports.py tests\experiments\test_phase8_simulation.py tests\experiments\test_run_all_cli.py tests\executors tests\plugins\factorization tests\test_phase7_ai_api_execution_flow.py -q` 通过，结果 `112 passed, 1 skipped in 67.69s`。真实模型预检：MiniMax anchor5 count5 为 `accuracy=0.6`；Step-3.5-Flash count3 快但 parse failed；GLM-5.2 count5 / timeout20 / max_tokens256 为 `accuracy=1.0`；GLM-5.2 worker10 count10 为 `accuracy=1.0`。正式真实运行 `$env:PYTHONPATH='src'; conda run -n tokenshare python -m tokenshare.experiments.run_factorization_500_ai --output-root outputs\experiments\factorization_500_ai_real_glm_seed1_count500_workers10_retry_20260702 --count 500 --seed 1 --real-transport --entry-id glm_5_2__sf_key_1 --max-provider-attempts 1 --max-tokens 256 --timeout-seconds 20 --worker-count 10` 退出码 0，报告 `attempted_count=500`、`correct_count=476`、`accuracy=0.952`、`parser_success_rate=0.954`、`raw_output_count=477`、`parsed_output_count=477`、`parse_failure_count=0`、`executor_error_count=23`、`failure_breakdown={"executor_error": 23, "product_mismatch": 1}`、`usage.total_tokens=525777`、`cost_estimate_total=1.7462418000000002`、`latency_ms_total=6741567`。最终完整 `.\init.ps1` 通过，pytest collected 388 items，结果 `387 passed, 1 skipped in 283.05s`。
